'use strict';

/**
 * address-of-stay controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::address-of-stay.address-of-stay');
