'use strict';

/**
 * organization-email-config router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::organization-email-config.organization-email-config');
