'use strict';

/**
 * organization-email-config controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::organization-email-config.organization-email-config');
