'use strict';

/**
 * organization-email-config service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::organization-email-config.organization-email-config');
