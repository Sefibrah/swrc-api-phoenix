'use strict';

/**
 * car-contract-fine service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::car-contract-fine.car-contract-fine');
