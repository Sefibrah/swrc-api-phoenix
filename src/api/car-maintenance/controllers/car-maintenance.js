'use strict';

/**
 * car-maintenance controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::car-maintenance.car-maintenance');
