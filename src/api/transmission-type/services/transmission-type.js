'use strict';

/**
 * transmission-type service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::transmission-type.transmission-type');
