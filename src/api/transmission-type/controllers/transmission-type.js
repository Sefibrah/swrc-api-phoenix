'use strict';

/**
 * transmission-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::transmission-type.transmission-type');
