'use strict';

/**
 * recurring-discount router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::recurring-discount.recurring-discount');
